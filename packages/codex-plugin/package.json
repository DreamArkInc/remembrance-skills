{
  "name": "@remembrance/codex-plugin",
  "version": "0.1.55",
  "private": true,
  "license": "Apache-2.0",
  "description": "OpenAI Codex CLI plugin for Remembrance auto-query hooks, skill docs, and MCP server configuration.",
  "type": "module",
  "scripts": {
    "build": "node --check scripts/session-start.mjs && node --check scripts/query-on-prompt.mjs && node --check scripts/contribute-on-stop.mjs && node --check scripts/record-detail-open.mjs && node --check scripts/hook-core.mjs",
    "test": "vitest run",
    "typecheck": "node --check scripts/session-start.mjs && node --check scripts/query-on-prompt.mjs && node --check scripts/contribute-on-stop.mjs && node --check scripts/record-detail-open.mjs && node --check scripts/hook-core.mjs"
  },
  "devDependencies": {
    "vitest": "^3.1.2"
  }
}
